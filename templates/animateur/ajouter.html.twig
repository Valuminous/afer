{% extends 'base.html.twig' %}

{% block body %}

    {% if editMode %}
    <h1>Modifier un outil</h1>
    {% else %}
    <h1>Ajouter un outil</h1>
    {% endif %}

{{ form_start(formAnimateur) }}

<button type="submit" class="btn btn-primary">
    {% if editMode %}
    Modifier
    {% else %}
    Sauvegarder
    {% endif %}
</button>

{{ form_end(formAnimateur) }}

{% endblock %}