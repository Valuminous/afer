{% extends 'base.html.twig' %}

{% block body %}

<section class="animateur">
    <div class="d-flex justify-content-between align-items-center">
        <h2 class="ml-5 h2_animateur">Liste des animateurs</h2>
        <a href="{{ path('animateur_ajouter')}}" class="btn mt-5 mb-5 mr-5 add_icon d-flex align-items-center">Ajouter</a>
    </div>
    <table class="table table-striped">
        <thead>
            <tr class="background_animateur">
                <th scope="col" class="text-center">Nom</th>
                <th scope="col" class="text-center">Prénom</th>
                <th scope="col" class="text-center">Email</th>
                <th scope="col" class="text-center">N° de portable</th>
                <th scope="col" class="text-center">N° fixe</th>
                <th scope="col" class="text-center"><i class="fas fa-cogs"></i></th>
            </tr>
        </thead>
        <tbody>
            {% for animateur in animateurs %}
            <tr>
                <td class="text-center">{{ animateur.nomAnimateur }}</td>
                <td class="text-center">{{ animateur.prenomAnimateur }}</td>
                <td class="text-center">{{ animateur.emailAnimateur }}</td>
                <td class="text-center">{{ animateur.numeroPortableAnimateur }}</td>
                <td class="text-center">{{ animateur.numeroFixeAnimateur }}</td>
                <td class="text-center">
                    <a href="{{  path('animateur_afficher', {'id': animateur.id}) }}"><i class="far fa-eye eye_color"></i></a>
                    <a href="{{  path('animateur_modifier', {'id': animateur.id}) }}"><i class="fas fa-pen pen_color"></i></a>
                    <a href="{{  path('animateur_supprimer', {'id': animateur.id}) }}"><i class="trash_color far fa-trash-alt"></i></a>
                </td>
            </tr>
            {% else %}
                <td class="text-center">Il n'y a aucun animateur</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
            {% endfor %}
        </tbody>
    </table>

</section>
{% endblock %}