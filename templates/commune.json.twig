[{% for commune in results -%}
    {{ {id: commune.id, label: commune.cp, value: commune.cp}|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]