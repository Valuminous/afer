{% extends 'base.html.twig' %}

{% block title %}Nouveau stagiaire{% endblock %}

{% block body %}

 <div class="container mt-4 text-center">
    <h1>Ajouter un stagiaire</h1>

     {{ include('stagiaire/form.html.twig', {form: form, button: 'Ajouter'})  }}
<div class="row ">
    <a href="{{ path('stagiaire_index') }}" class="btn btn-dark">Retour</a>
    </div>
      </div>

    
        </tbody>
    </table>


{% endblock %}
