{{ form_start(formStagiaire) }}
<div class="row">
  <div class="col-md-6">{{ form_row(formStagiaire.nomStagiaire) }}</div>
{{ form_end(form) }}
    <div class="col-md-6">{{ form_row(formStagiaire.prenomStagiaire) }}</div>
</div>
    {{ form_widget(form) }}
    <button class="btn btn-info">{{ button|default('Modifier') }}</button>
{{ form_end(formStagiaire) }}
