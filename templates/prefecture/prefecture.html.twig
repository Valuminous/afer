{% extends 'base.html.twig' %}

{% block body %}

<section class="prefecture">
        <div class="d-flex justify-content-between align-items-center">
            <h2 class="ml-5 h2_prefecture">Liste des préfectures</h2>
            <a href="{{ path('prefecture_ajouter') }}" class="btn mt-5 mb-5 mr-5 add_icon d-flex align-items-center">Ajouter</a>
        </div>

    <table class="table table-striped">
        <thead>
            <tr class="background_prefecture">
                <th scope="col" class="text-left pl-5">Nom</th>
                <th scope="col" class="text-left">Commune</th>
                <th scope="col" class="text-left">Autorité</th>
                <th scope="col" class="text-left">Service</th>
                <th scope="col" class="text-center pr-5"><i class="fas fa-cogs"></i></th>
            </tr>
        </thead>

        <tbody>
            {% for prefecture in prefectures %}
                <tr>
                    <td class="text-left pl-5">{{ prefecture.nomPrefecture }}</td>
                    <td class="text-left">{{ prefecture.communePrefecture }}</td>
                      <td class="text-left">{{ prefecture.prefectureAutorite }}</td>
                    <td class="text-left">{{ prefecture.prefectureService }}</td>
                    <td class="text-center">
                        <a href="{{ path('prefecture_afficher', {'id': prefecture.id}) }}"><i class="ml-3 fas fa-eye eye_color"></i></a>
                        <a href="{{ path('prefecture_modifier', {'id': prefecture.id}) }}"><i class="ml-3 fas fa-pen pen_color"></i></a>
                        <a href="{{ path('prefecture_supprimer', {'id': prefecture.id}) }}"><i class="ml-3 far fa-trash-alt trash_color pr-5"></i></a>
                    </td>
                </tr>
            {% else %}
                <td class="pl-5 text-left">Il n'y a aucune préfecture</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
                <td class="text-center">...</td>
            {% endfor %}
        </tbody>
    </table>
</section>

{% endblock %}
