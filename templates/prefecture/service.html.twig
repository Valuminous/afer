{% extends 'base.html.twig' %}

{% block title %}Préfecture{% endblock %}

{% block body %}

<a href="{{ path('prefectureService_ajouter') }}" class="btn btn-primary btn-link text-white ml-2"><i class="fas fa-plus-circle pr-2"></i> Ajouter</a>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Nom</th>
                <th scope="col">Réglages</th>
            </tr>
        </thead>

        <tbody>
            {% for service in services %}
                <tr>
                    <td>{{ service.nom }}</td>
                    <td>
                        <a href="{{ path('prefectureService_modifier', {'id': service.id}) }}"><i class="fas fa-pencil-alt"></i></a>
                        <a href="{{ path('prefectureService_supprimer', {'id': service.id}) }}"><i class="fas fa-trash-alt"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
