{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(formPrefectureAutorite) }}

    {{ form_row(formPrefectureAutorite.nom, {'label': 'Nom', 'attr': {'placeholder': 'Nom'} }) }}

    <button type="submit" class="btn btn-primary btn-link text-white mb-5 mt-3">
        {% if editMode %}
            Enregistrer
        {% else %}
            Valider
        {% endif %}
    </button>

    {{ form_end(formPrefectureAutorite) }}

{% endblock %}