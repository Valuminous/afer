{% extends "base.html.twig" %}

{% block title %}Ajouter une autorite de tribunal{% endblock %}

{% block body %}  

    <div>
        <h2>Ajouter une autorite de tribunal</h2>
       
        <div class="content">
            
            <div>
                    {{ form_start(form) }}

                        {{ form_row(form.nom, {'label':"Nom de l'autorite du tribulal", 'attr': {'placeholder': "Nom de l'autorite du tribulal"}}) }}
    
                        <button type="submit" class="btn btn-success">
                            {% if  editMode %}
                            Enregistrer les modifications
                            {% else %}
                                Ajouter l'autorite  
                            {% endif %} 
                        </button>

                    {{ form_end(form) }}
            </div>
            
        </div>
    </div>


{% endblock %}